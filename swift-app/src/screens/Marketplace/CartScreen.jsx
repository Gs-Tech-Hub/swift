import React from 'react'; import { View, Text, ScrollView } from 'react-native'; import { useMarketplace } from '../../contexts/MarketplaceContext'; import Button from '../../components/Button';
export default function CartScreen({navigation}){ const { cart, clearCart } = useMarketplace(); const total = cart.reduce((s,p)=> s + (p.price||0), 0); return (<ScrollView style={{flex:1,padding:20}}><Text style={{fontSize:22,fontWeight:'700'}}>Cart</Text>{cart.map(c=> <View key={c.id}><Text>{c.name} - ₦{c.price}</Text></View>)}<Text style={{marginTop:10}}>Total: ₦{total}</Text><View style={{marginTop:10}}><Button title='Checkout' onPress={()=>navigation.navigate('Checkout')}/></View><View style={{marginTop:6}}><Button title='Clear' variant='outline' onPress={clearCart}/></View></ScrollView>); }
